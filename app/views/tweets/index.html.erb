<h1>ツイート一覧</h1>

<% @tweets.each do |e| %>
  <%= e.message %>
  <%= e.user_id %>
  <%= link_to "削除", tweet_path(e), data: {turbo_method: :delete} %>
  <br>
<% end %>
<br>
<%= link_to "新規ツイート", new_tweet_path %>